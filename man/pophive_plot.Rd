% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main-user-function.R
\name{pophive_plot}
\alias{pophive_plot}
\title{Create PopHIVE Styled Plot}
\usage{
pophive_plot(
  data,
  plot_type = c("map", "line", "dot", "bar", "col"),
  aes_mappings,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  x_label = NULL,
  y_label = NULL,
  legend_title = "Legend",
  position = "dodge",
  bar_width = 0.7,
  line_width = 1,
  date_breaks = NULL,
  date_labels = NULL,
  y_scale_type = "comma",
  dot_color = "#4e98e4",
  dot_size = 4,
  ...
)
}
\arguments{
\item{data}{A data frame or sf object containing the data to plot.}

\item{plot_type}{Character string specifying the type of plot to create.
Options are: "map", "line", "dot", "bar", "col" (same as "bar").}

\item{aes_mappings}{A list of aesthetic mappings created with \code{aes()}.
Required mappings depend on plot type:
\itemize{
\item map: \code{fill} (and geometry from sf object)
\item line: \code{x}, \code{y}, \code{color}, \code{linetype} (color and linetype usually the same variable)
\item dot: \code{x}, \code{y}
\item bar/col: \code{x}, \code{y}, \code{fill}
}}

\item{title}{Character string for the plot title. Default is NULL.}

\item{subtitle}{Character string for the plot subtitle. Default is NULL.}

\item{caption}{Character string for the plot caption. Default is NULL.}

\item{x_label}{Character string for the x-axis label. Default is NULL.}

\item{y_label}{Character string for the y-axis label. Default is NULL.}

\item{legend_title}{Character string for the legend title. Used only for map plots.
Default is "Legend".}

\item{position}{For bar/col charts, the position adjustment. Options are
"dodge", "stack", or "fill". Default is "dodge".}

\item{bar_width}{Numeric value for the width of bars in bar/col charts.
Default is 0.7.}

\item{line_width}{Numeric value for the width of lines in line plots.
Default is 1.}

\item{date_breaks}{For line plots with date x-axis, the spacing of date breaks
(e.g., "1 month", "1 year"). Default is NULL (automatic).}

\item{date_labels}{For line plots with date x-axis, the format of date labels
(e.g., "\%b \%Y", "\%Y"). Default is NULL (automatic).}

\item{y_scale_type}{For line plots, the type of y-axis scale transformation
from the scales package (e.g., "comma", "percent", "dollar"). Default is "comma".}

\item{dot_color}{Character string for the color of dots in dot plots.
Default is "#4e98e4" (PopHIVE blue).}

\item{dot_size}{Numeric value for the size of dots in dot plots. Default is 4.}

\item{...}{Additional arguments passed to the geom layer.}
}
\value{
A ggplot2 object with PopHIVE styling applied.
}
\description{
Main function to create publication-ready plots with PopHIVE brand styling.
This is the primary user-facing function that handles all plot types.
}
\details{
This function automatically applies the appropriate PopHIVE theme and color
scales based on the plot type. Make sure to call \code{setup_pophive_fonts()}
at the start of your R session before creating plots.

Plot types:
\itemize{
\item "map": Choropleth maps using sf objects
\item "line": Line graphs with time series or continuous data
\item "dot": Horizontal dot plots for comparisons
\item "bar" or "col": Bar/column charts for categorical comparisons
}
}
\examples{
\dontrun{
# Load fonts first
library(pophive)
setup_pophive_fonts()

# Choropleth map
library(sf)
pophive_plot(
  data = states_sf,
  plot_type = "map",
  aes_mappings = aes(fill = population),
  title = "US Population by State",
  subtitle = "2024 estimates",
  caption = "Source: Census Bureau",
  legend_title = "Population"
)

# Line graph
pophive_plot(
  data = time_series,
  plot_type = "line",
  aes_mappings = aes(x = year, y = value, color = category, linetype = category),
  title = "Trends Over Time",
  x_label = "Year",
  y_label = "Value",
  line_width = 0.8
)

# Dot plot
pophive_plot(
  data = comparison_data,
  plot_type = "dot",
  aes_mappings = aes(x = value, y = reorder(category, value)),
  title = "Category Comparison",
  x_label = "Value",
  y_label = "Category"
)

# Bar chart
pophive_plot(
  data = sales_data,
  plot_type = "col",
  aes_mappings = aes(x = quarter, y = sales, fill = product),
  title = "Quarterly Sales",
  x_label = "Quarter",
  y_label = "Sales (thousands)",
  position = "dodge"
)
}

}
